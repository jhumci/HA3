from calculation import calc_mean
from my_functions import estimate_max_hr, build_person, build_experiment

if __name__ == "__main__":
    liste = [1,2,3]

    print(calc_mean(liste))

    # Mittelwert berechnen
